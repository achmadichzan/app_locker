[package]
name = "ui"
version.workspace = true
edition.workspace = true

[dependencies]
app_core = { path = "../core" }
slint = "1.15.1"
serde_json = "1.0"

[build-dependencies]
slint-build = "1.15.1"

[dependencies.windows]
version = "0.52"
features = [
    "Win32_UI_WindowsAndMessaging",
]